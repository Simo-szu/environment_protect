<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youthloop.game.persistence.mapper.GameActionMapper">

    <resultMap id="BaseResultMap" type="com.youthloop.game.persistence.entity.GameActionEntity">
        <id column="id" property="id" jdbcType="OTHER" typeHandler="com.youthloop.common.mybatis.UuidTypeHandler"/>
        <result column="session_id" property="sessionId" jdbcType="OTHER" typeHandler="com.youthloop.common.mybatis.UuidTypeHandler"/>
        <result column="user_id" property="userId" jdbcType="OTHER" typeHandler="com.youthloop.common.mybatis.UuidTypeHandler"/>
        <result column="action_type" property="actionType" jdbcType="INTEGER"/>
        <result column="action_data" property="actionData" jdbcType="OTHER" typeHandler="com.youthloop.common.mybatis.JsonNodeTypeHandler"/>
        <result column="points_earned" property="pointsEarned" jdbcType="INTEGER"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP_WITH_TIMEZONE"/>
    </resultMap>
    
    <insert id="insert">
        INSERT INTO game.game_action (id, session_id, user_id, action_type, action_data, points_earned, created_at)
        VALUES (
            #{id, jdbcType=OTHER, typeHandler=com.youthloop.common.mybatis.UuidTypeHandler},
            #{sessionId, jdbcType=OTHER, typeHandler=com.youthloop.common.mybatis.UuidTypeHandler},
            #{userId, jdbcType=OTHER, typeHandler=com.youthloop.common.mybatis.UuidTypeHandler},
            #{actionType, jdbcType=INTEGER},
            #{actionData, jdbcType=OTHER, typeHandler=com.youthloop.common.mybatis.JsonNodeTypeHandler},
            #{pointsEarned, jdbcType=INTEGER},
            #{createdAt, jdbcType=TIMESTAMP_WITH_TIMEZONE}
        )
    </insert>

    <select id="countBySessionId" resultType="long">
        SELECT COUNT(1)
        FROM game.game_action
        WHERE session_id = #{sessionId, jdbcType=OTHER, typeHandler=com.youthloop.common.mybatis.UuidTypeHandler}
    </select>

    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT id, session_id, user_id, action_type, action_data, points_earned, created_at
        FROM game.game_action
        WHERE session_id = #{sessionId, jdbcType=OTHER, typeHandler=com.youthloop.common.mybatis.UuidTypeHandler}
        ORDER BY created_at DESC
        LIMIT #{limit, jdbcType=INTEGER}
        OFFSET #{offset, jdbcType=INTEGER}
    </select>

</mapper>
