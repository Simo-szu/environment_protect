<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.youthloop.interaction.persistence.mapper.CommentMapper">

  <!-- Result Map -->
  <resultMap id="BaseResultMap" type="com.youthloop.interaction.persistence.entity.CommentEntity">
    <id column="id" property="id" jdbcType="OTHER" typeHandler="org.apache.ibatis.type.UUIDTypeHandler"/>
    <result column="target_type" property="targetType" jdbcType="INTEGER"/>
    <result column="target_id" property="targetId" jdbcType="OTHER" typeHandler="org.apache.ibatis.type.UUIDTypeHandler"/>
    <result column="user_id" property="userId" jdbcType="OTHER" typeHandler="org.apache.ibatis.type.UUIDTypeHandler"/>
    <result column="parent_id" property="parentId" jdbcType="OTHER" typeHandler="org.apache.ibatis.type.UUIDTypeHandler"/>
    <result column="root_id" property="rootId" jdbcType="OTHER" typeHandler="org.apache.ibatis.type.UUIDTypeHandler"/>
    <result column="depth" property="depth" jdbcType="INTEGER"/>
    <result column="body" property="body" jdbcType="VARCHAR"/>
    <result column="status" property="status" jdbcType="INTEGER"/>
    <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    <result column="updated_at" property="updatedAt" jdbcType="TIMESTAMP"/>
  </resultMap>

  <!-- 插入评论 -->
  <insert id="insert">
    INSERT INTO social.comment (
      id, target_type, target_id, user_id, parent_id, root_id, depth, body, status, created_at, updated_at
    ) VALUES (
      #{id}::uuid, #{targetType}, #{targetId}::uuid, #{userId}::uuid, 
      #{parentId}::uuid, #{rootId}::uuid, #{depth}, #{body}, #{status}, #{createdAt}, #{updatedAt}
    )
  </insert>

  <!-- 根据 ID 查询 -->
  <select id="selectById" resultMap="BaseResultMap">
    SELECT id, target_type, target_id, user_id, parent_id, root_id, depth, body, status, created_at, updated_at
    FROM social.comment
    WHERE id = #{id}::uuid
  </select>

  <!-- 查询目标的评论列表 -->
  <select id="selectByTarget" resultMap="BaseResultMap">
    SELECT id, target_type, target_id, user_id, parent_id, root_id, depth, body, status, created_at, updated_at
    FROM social.comment
    WHERE target_type = #{targetType}
      AND target_id = #{targetId}::uuid
      AND status = 1
    ORDER BY created_at DESC
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <!-- 统计目标的评论总数 -->
  <select id="countByTarget" resultType="java.lang.Long">
    SELECT COUNT(*)
    FROM social.comment
    WHERE target_type = #{targetType}
      AND target_id = #{targetId}::uuid
      AND status = 1
  </select>

  <!-- 查询根评论下的回复 -->
  <select id="selectByRoot" resultMap="BaseResultMap">
    SELECT id, target_type, target_id, user_id, parent_id, root_id, depth, body, status, created_at, updated_at
    FROM social.comment
    WHERE root_id = #{rootId}::uuid
      AND status = 1
    ORDER BY created_at ASC
    LIMIT #{limit} OFFSET #{offset}
  </select>

  <!-- 更新状态 -->
  <update id="updateStatus">
    UPDATE social.comment
    SET status = #{status},
        updated_at = now()
    WHERE id = #{id}::uuid
  </update>

</mapper>
