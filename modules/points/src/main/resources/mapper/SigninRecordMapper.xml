<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youthloop.points.persistence.mapper.SigninRecordMapper">
    
    <resultMap id="BaseResultMap" type="com.youthloop.points.persistence.entity.SigninRecordEntity">
        <id column="user_id" property="userId" jdbcType="OTHER" typeHandler="com.youthloop.common.mybatis.UuidTypeHandler"/>
        <id column="signin_date" property="signinDate" jdbcType="DATE"/>
        <result column="is_signed" property="isSigned" jdbcType="BOOLEAN"/>
        <result column="signed_at" property="signedAt" jdbcType="TIMESTAMP"/>
        <result column="is_makeup" property="isMakeup" jdbcType="BOOLEAN"/>
        <result column="streak_count" property="streakCount" jdbcType="INTEGER"/>
        <result column="created_at" property="createdAt" jdbcType="TIMESTAMP"/>
    </resultMap>
    
    <select id="selectByUserIdAndDate" resultMap="BaseResultMap">
        SELECT user_id, signin_date, is_signed, signed_at, is_makeup, streak_count, created_at
        FROM social.signin_record
        WHERE user_id = #{userId, jdbcType=OTHER, typeHandler=com.youthloop.common.mybatis.UuidTypeHandler}
          AND signin_date = #{signinDate, jdbcType=DATE}
    </select>
    
    <select id="selectLatestByUserId" resultMap="BaseResultMap">
        SELECT user_id, signin_date, is_signed, signed_at, is_makeup, streak_count, created_at
        FROM social.signin_record
        WHERE user_id = #{userId, jdbcType=OTHER, typeHandler=com.youthloop.common.mybatis.UuidTypeHandler}
        ORDER BY signin_date DESC
        LIMIT 1
    </select>
    
    <insert id="insert">
        INSERT INTO social.signin_record (user_id, signin_date, is_signed, signed_at, is_makeup, streak_count, created_at)
        VALUES (
            #{userId, jdbcType=OTHER, typeHandler=com.youthloop.common.mybatis.UuidTypeHandler},
            #{signinDate, jdbcType=DATE},
            #{isSigned, jdbcType=BOOLEAN},
            #{signedAt, jdbcType=TIMESTAMP},
            #{isMakeup, jdbcType=BOOLEAN},
            #{streakCount, jdbcType=INTEGER},
            #{createdAt, jdbcType=TIMESTAMP}
        )
    </insert>
    
</mapper>
